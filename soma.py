def soma(a: float, b:float = 40) -> float:
    """ Sum two values.

    Take two variables ad sum.

    Args:
        a (float): first number.
        b (float, optional): second number. Defaults to 5.

    Returns:
        float: The sum of 'a' and 'b'.
    """
    numero = a + b
   
    return numero